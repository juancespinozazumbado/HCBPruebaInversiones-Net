create or replace PACKAGE PCK_HCB_INVERSIONES AS 

  /* TODO enter package declarations (types, exceptions, methods etc) here */
  
 PROCEDURE AGREGAR_INVERSION(
    monto number, 
    tasa_interes Decimal,
    plazo number,
    cupones_anual NUMBER);
    
    
  PROCEDURE MODIFICAR_INVERSION(
    Id number,
    monto_inversion number, 
    tasa_interes_anual DECIMAL,
    plazo_meses number,
    cant_cupones_anual NUMBER);
    
  PROCEDURE ELIMINAR_INVERSION(Id_inversion number);
  
  PROCEDURE LISTAR_INVERSIONES( inversiones out SYS_REFCURSOR );
  
  
   PROCEDURE LISTAR_INVERSIONES_DETALLES(Id_inversion number,  inversionesDetalles out SYS_REFCURSOR );
   PROCEDURE LISTAR_ENCABEZADO (Id_inversion number,  Encabezado out SYS_REFCURSOR );
   
   PROCEDURE GENERAR_CUPONES(inversion_id number, resultado out SYS_REFCURSOR);
    
  

END PCK_HCB_INVERSIONES;